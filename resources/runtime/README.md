# This folder is no longer used

The Mod Loader runtime is now embedded into `GMM.exe` from the
[`runtime/`](../../runtime/README.md) folder instead (recursively, whole tree).
See its `README.md` for details on what to put there.
